#include "main.h"

/**
 * mul - makes the multiplication
 * @a: first value to be checked
 * @b: second value to be checked
 *
 * Return: multiplication of both a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
